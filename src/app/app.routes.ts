import { Routes } from '@angular/router';
import { TopBar } from '../components/layout/topbar/top-bar.component';

export const routes: Routes = [
    {
        path: '',
        component: TopBar
    }
];
